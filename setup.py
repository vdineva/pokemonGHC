from setuptools import setup
setup(name='pokestarter',
      version='0.1',
      description='API and UI for deciding which Pokemon starter to choose',
      url='https://github.com/BeenzSyed/pokemonGHC',
      packages=['pokestarter'],
      include_package_data=True,
      install_requires=['Flask==0.11'])
